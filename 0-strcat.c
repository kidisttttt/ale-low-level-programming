#include<studio.h>
#include<srting.h>
int main(){
char string[50]
char string[50]
print("Enter the first string:");
gets(string1);
print("Enter the second string:);
gets(string2);
strcat(string1,string2);
print("string concatenation is %s \n",string1);
return 0;
}
